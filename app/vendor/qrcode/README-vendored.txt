python-qrcode 7.4.2 (BSD-3-Clause, Lincoln Loop — see LICENSE), vendored so the
2FA setup page can show a scannable QR code without a package install.
Trimmed to the SVG path: PNG/PIL image factories removed, typing_extensions
replaced by typing.Literal, imports rewritten to app.vendor.qrcode.
